import { MdcDialogDirective } from './mdc-dialog.directive';

describe('MdcDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new MdcDialogDirective();
    expect(directive).toBeTruthy();
  });
});

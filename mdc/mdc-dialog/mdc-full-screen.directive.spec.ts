import { MdcFullScreenDirective } from './mdc-full-screen.directive';

describe('MdcFullScreenDirective', () => {
  it('should create an instance', () => {
    const directive = new MdcFullScreenDirective();
    expect(directive).toBeTruthy();
  });
});

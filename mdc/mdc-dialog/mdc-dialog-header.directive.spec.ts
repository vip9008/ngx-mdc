import { MdcDialogHeaderDirective } from './mdc-dialog-header.directive';

describe('MdcDialogHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new MdcDialogHeaderDirective();
    expect(directive).toBeTruthy();
  });
});

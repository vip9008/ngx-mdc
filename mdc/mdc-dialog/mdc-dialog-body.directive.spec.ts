import { MdcDialogBodyDirective } from './mdc-dialog-body.directive';

describe('MdcDialogBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new MdcDialogBodyDirective();
    expect(directive).toBeTruthy();
  });
});

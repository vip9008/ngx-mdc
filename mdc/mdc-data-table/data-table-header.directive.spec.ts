import { DataTableHeaderDirective } from './data-table-header.directive';

describe('DataTableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new DataTableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});

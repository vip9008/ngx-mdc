import { DataTableFooterDirective } from './data-table-footer.directive';

describe('DataTableFooterDirective', () => {
  it('should create an instance', () => {
    const directive = new DataTableFooterDirective();
    expect(directive).toBeTruthy();
  });
});

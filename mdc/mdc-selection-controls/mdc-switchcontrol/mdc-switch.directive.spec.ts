import { MdcSwitchDirective } from './mdc-switch.directive';

describe('MdcSwitchDirective', () => {
  it('should create an instance', () => {
    const directive = new MdcSwitchDirective();
    expect(directive).toBeTruthy();
  });
});

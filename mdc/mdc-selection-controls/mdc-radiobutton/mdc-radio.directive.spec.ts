import { MdcRadioDirective } from './mdc-radio.directive';

describe('MdcRadioDirective', () => {
  it('should create an instance', () => {
    const directive = new MdcRadioDirective();
    expect(directive).toBeTruthy();
  });
});

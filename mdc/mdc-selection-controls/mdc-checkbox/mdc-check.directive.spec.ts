import { MdcCheckDirective } from './mdc-check.directive';

describe('MdcCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new MdcCheckDirective();
    expect(directive).toBeTruthy();
  });
});

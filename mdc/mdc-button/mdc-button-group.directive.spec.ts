import { MdcButtonGroupDirective } from './mdc-button-group.directive';

describe('MdcButtonGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new MdcButtonGroupDirective();
    expect(directive).toBeTruthy();
  });
});

import { MdcFabLabelDirective } from './mdc-fab-label.directive';

describe('MdcFabLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new MdcFabLabelDirective();
    expect(directive).toBeTruthy();
  });
});

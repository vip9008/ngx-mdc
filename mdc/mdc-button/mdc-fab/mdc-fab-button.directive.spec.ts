import { MdcFabButtonDirective } from './mdc-fab-button.directive';

describe('MdcFabButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new MdcFabButtonDirective();
    expect(directive).toBeTruthy();
  });
});

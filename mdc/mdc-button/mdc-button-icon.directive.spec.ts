import { MdcButtonIconDirective } from './mdc-button-icon.directive';

describe('MdcButtonIconDirective', () => {
  it('should create an instance', () => {
    const directive = new MdcButtonIconDirective();
    expect(directive).toBeTruthy();
  });
});

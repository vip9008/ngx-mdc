import { MdcMenuCloseDirective } from './mdc-menu-close.directive';

describe('MdcMenuCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new MdcMenuCloseDirective();
    expect(directive).toBeTruthy();
  });
});

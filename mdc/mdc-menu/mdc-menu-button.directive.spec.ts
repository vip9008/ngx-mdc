import { MdcMenuButtonDirective } from './mdc-menu-button.directive';

describe('MdcMenuButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new MdcMenuButtonDirective();
    expect(directive).toBeTruthy();
  });
});

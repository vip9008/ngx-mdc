import { ListItemActionDirective } from './list-item-action.directive';

describe('ListItemActionDirective', () => {
  it('should create an instance', () => {
    const directive = new ListItemActionDirective();
    expect(directive).toBeTruthy();
  });
});

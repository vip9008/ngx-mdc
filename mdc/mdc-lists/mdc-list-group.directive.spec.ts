import { MdcListGroupDirective } from './mdc-list-group.directive';

describe('MdcListGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new MdcListGroupDirective();
    expect(directive).toBeTruthy();
  });
});

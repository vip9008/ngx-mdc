import { MdcDropdownListDirective } from './mdc-dropdown-list.directive';

describe('MdcDropdownListDirective', () => {
  it('should create an instance', () => {
    const directive = new MdcDropdownListDirective();
    expect(directive).toBeTruthy();
  });
});

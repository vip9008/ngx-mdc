import { MdcListTextDirective } from './mdc-list-text.directive';

describe('MdcListTextDirective', () => {
  it('should create an instance', () => {
    const directive = new MdcListTextDirective();
    expect(directive).toBeTruthy();
  });
});

import { SliderInputDirective } from './slider-input.directive';

describe('SliderInputDirective', () => {
  it('should create an instance', () => {
    const directive = new SliderInputDirective();
    expect(directive).toBeTruthy();
  });
});

import { MdcFullWidthDirective } from './mdc-full-width.directive';

describe('FullWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new MdcFullWidthDirective();
    expect(directive).toBeTruthy();
  });
});

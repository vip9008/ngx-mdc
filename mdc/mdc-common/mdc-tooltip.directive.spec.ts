import { MdcTooltipDirective } from './mdc-tooltip.directive';

describe('MdcTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new MdcTooltipDirective();
    expect(directive).toBeTruthy();
  });
});

import { MdcIconElementDirective } from './mdc-icon-element.directive';

describe('MdcIconElementDirective', () => {
  it('should create an instance', () => {
    const directive = new MdcIconElementDirective();
    expect(directive).toBeTruthy();
  });
});

import { MdcHostDirective } from './mdc-host.directive';

describe('MdcHostDirective', () => {
  it('should create an instance', () => {
    const directive = new MdcHostDirective();
    expect(directive).toBeTruthy();
  });
});

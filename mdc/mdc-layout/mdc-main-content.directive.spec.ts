import { MdcMainContentDirective } from './mdc-main-content.directive';

describe('MdcMainContentDirective', () => {
  it('should create an instance', () => {
    const directive = new MdcMainContentDirective();
    expect(directive).toBeTruthy();
  });
});

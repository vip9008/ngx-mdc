import { MdcTopAppBarDirective } from './mdc-top-app-bar.directive';

describe('MdcTopAppBarDirective', () => {
  it('should create an instance', () => {
    const directive = new MdcTopAppBarDirective();
    expect(directive).toBeTruthy();
  });
});

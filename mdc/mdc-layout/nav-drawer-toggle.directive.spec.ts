import { NavDrawerToggleDirective } from './nav-drawer-toggle.directive';

describe('NavDrawerToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new NavDrawerToggleDirective();
    expect(directive).toBeTruthy();
  });
});
